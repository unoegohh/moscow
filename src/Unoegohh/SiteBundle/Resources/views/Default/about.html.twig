{% set url="/about" %}
{% extends "UnoegohhSiteBundle:Default:index.html.twig" %}

{% block main %}
    <div id="about_page" style="font-family: Thinn">
        {% if _locale == "ru" %}
            {{ page.text | raw }}
        {% else %}
            {{ page.textEng | raw }}
        {% endif %}
    </div>
{% endblock %}